#include <player.h>

namespace model {
    
    Player::Player() {

    };

    Player::~Player() {

    };

    void Player::draw(Renderer * renderer) {

    };

    void Player::update(Uint32 dt, const Uint8 * input) {

    };
}
