#include <vector>
#include <abstract_object.h>

namespace factory {
    class ObjectFactory {
        public:
            ObjectFactory();
            ~ObjectFactory();
        private:
            
    };
}